# 购物车功能开发计划

## 目标

完成用户端购物车完整业务：添加商品、查询购物车、减少商品数量、清空购物车，并确保不同用户的数据相互隔离。

## 实施步骤

### 1. 新增购物车数据访问层

- 根据用户、菜品/套餐及口味查询购物车中的同款商品。
- 新增购物车记录、更新商品数量、删除单条记录。
- 按当前用户查询和清空购物车。
- 涉及文件：
  - `sky-server/src/main/java/com/sky/mapper/ShoppingCartMapper.java`

### 2. 新增购物车业务层

- 添加商品：同款商品已存在时数量加一，否则读取菜品或套餐信息并新增记录。
- 减少商品：数量大于一时减一，数量等于一时删除记录。
- 查询和清空操作始终使用当前登录用户 ID，避免越权访问。
- 涉及文件：
  - `sky-server/src/main/java/com/sky/service/ShoppingCartService.java`
  - `sky-server/src/main/java/com/sky/service/impl/ShoppingCartServiceImpl.java`

### 3. 新增用户端接口

- `POST /user/shoppingCart/add`：添加商品。
- `GET /user/shoppingCart/list`：查看购物车。
- `POST /user/shoppingCart/sub`：减少商品数量。
- `DELETE /user/shoppingCart/clean`：清空购物车。
- 涉及文件：
  - `sky-server/src/main/java/com/sky/controller/user/ShoppingCartController.java`

### 4. 验证

- 编译全部 Maven 模块，检查接口、依赖注入与 MyBatis 映射是否合法。
- 检查变更范围，确保未修改购物车业务以外的功能。

