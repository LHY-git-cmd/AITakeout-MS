管理端 Agent 第一版剩余事项（模块一完成后）

P0：管理端消费 SSE 事件序号，实现事件去重、有限断线重连、Last-Event-ID 续传和 Java/MySQL 最终状态校验。

P1：将静态“Agent 服务在线”替换为 Agent、Qdrant、Embedding 和 LLM 的真实正常、降级、不可用状态。

P1：按 HTTP 状态和结构化错误建立管理端错误分类、恢复提示和可重试动作。

P1：PermissionDeniedException 已返回 HTTP 403；继续将 Agent 任务、会话、知识库和文档归属拒绝从通用业务异常统一为 403，同时保持安全文案。

P0：建立不少于 40 条的 Agent 工具调用专项评测，覆盖工具选择、参数、多步骤、确认、越权和工具失败，并执行真实模型抽样。

P0：在完整单机 Docker Compose 环境执行 12 类端到端场景并保存可定位证据。

P1：更新历史 RAG、Agent 链路、工具调用、质量基线和运维文档，消除已经失效的缺口描述。

发布：执行管理端、Java、Python、RAG、工具评测、Compose 冒烟、备份恢复和回滚检查，生成第一版最终实施报告。
