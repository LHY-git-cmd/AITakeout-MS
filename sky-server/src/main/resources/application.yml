server:
  port: 8080

springdoc:
  api-docs:
    enabled: true
    path: /v3/api-docs
  swagger-ui:
    enabled: true
    path: /swagger-ui.html
    tags-sorter: alpha
    operations-sorter: alpha

spring:
  profiles:
    active: dev
  datasource:
    druid:
      driver-class-name: ${sky.datasource.driver-class-name}
      url: jdbc:mysql://${sky.datasource.host}:${sky.datasource.port}/${sky.datasource.database}?serverTimezone=Asia/Shanghai&useUnicode=true&characterEncoding=utf-8&zeroDateTimeBehavior=convertToNull&useSSL=false&allowPublicKeyRetrieval=true
      username: ${sky.datasource.username}
      password: ${sky.datasource.password}
      validation-query: SELECT 1
  data:
    redis:
      host: ${sky.redis.host}
      port: ${sky.redis.port}
      password: ${sky.redis.password}
      database: ${sky.redis.database}
  flyway:
    enabled: true
    baseline-on-migrate: true
    baseline-version: "0"
    locations: classpath:db/migration
    validate-on-migrate: true
  ai:
    model:
      chat: ${AI_CHAT_MODEL:none}
      embedding: none
      image: none
      audio:
        transcription: none
        speech: none
      moderation: none
    chat:
      client:
        enabled: ${AI_ENABLED:false}
    openai:
      api-key: ${DEEPSEEK_API_KEY:}
      base-url: ${DEEPSEEK_BASE_URL:https://api.deepseek.com}
      chat:
        completions-path: /v1/chat/completions
        options:
          model: ${DEEPSEEK_MODEL:deepseek-v4-pro}
          reasoning-effort: ${DEEPSEEK_REASONING_EFFORT:high}

mybatis:
  #mapper配置文件
  mapper-locations: classpath:mapper/*.xml
  type-aliases-package: com.sky.entity
  configuration:
    #开启驼峰命名
    map-underscore-to-camel-case: true

logging:
  level:
    com:
      sky:
        mapper: info
        service: info
        controller: info

sky:
  web-login:
    enabled: false
  cors:
    allowed-origins: ${CORS_ALLOWED_ORIGINS:http://localhost:9528,http://localhost:8081}
  jwt:
    # 设置jwt签名加密时使用的秘钥（至少32个字符）
    admin-secret-key: itcast-itcast-itcast-itcast-itcast
    # 设置jwt过期时间
    admin-ttl: 7200000
    # 设置前端传递过来的令牌名称
    admin-token-name: token
    user-secret-key: itheima-itheima-itheima-itheima-itheima
    user-ttl: 7200000
    user-token-name: authentication
  alioss:
    endpoint: ${sky.alioss.endpoint}
    access-key-secret: ${sky.alioss.access-key-secret}
    access-key-id: ${sky.alioss.access-key-id}
    bucket-name: ${sky.alioss.bucket-name}
  wechat:
    appid: ${sky.wechat.appid}
    secret: ${sky.wechat.secret}
  shop:
    address: ${SHOP_ADDRESS:北京市海淀区上地十街10号}
  baidu:
    ak: ${BAIDU_MAP_AK:}
  delivery:
    max-distance: 5000
  order-task:
    batch-size: ${ORDER_TASK_BATCH_SIZE:200}
  ai:
    enabled: ${AI_ENABLED:false}
    daily-limit: 10
    context-message-limit: 20
    retention-days: 90
    generation-timeout: 60s