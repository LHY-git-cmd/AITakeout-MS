<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.sky.mapper.SetmealDishMapper">

    <!-- 根据菜品id查询关联的套餐ID列表 -->
    <select id="getSetmealIdsByDishId" resultType="java.lang.Long">
        select setmeal_id from setmeal_dish where dish_id = #{dishId}
    </select>

    <!-- 批量插入套餐菜品关系 -->
    <insert id="insertBatch">
        insert into setmeal_dish (setmeal_id, dish_id, name, price, copies, create_time, update_time, create_user, update_user)
        values
        <foreach collection="list" item="item" separator=",">
            (#{item.setmealId}, #{item.dishId}, #{item.name}, #{item.price}, #{item.copies}, #{item.createTime}, #{item.updateTime}, #{item.createUser}, #{item.updateUser})
        </foreach>
    </insert>

    <!-- 根据套餐id查询菜品列表 -->
    <select id="getBySetmealId" resultType="com.sky.entity.SetmealDish">
        select * from setmeal_dish where setmeal_id = #{setmealId}
    </select>

    <!-- 批量删除套餐菜品关系 -->
    <delete id="deleteBySetmealIds">
        DELETE FROM setmeal_dish WHERE setmeal_id IN
        <foreach collection="setmealIds" item="setmealId" open="(" separator="," close=")">
            #{setmealId}
        </foreach>
    </delete>

</mapper>