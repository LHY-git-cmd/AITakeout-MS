<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.sky.mapper.AgentEventMapper">

    <insert id="insert" parameterType="com.sky.entity.AgentEvent"
            useGeneratedKeys="true" keyProperty="id">
        insert into agent_event
        (event_id, task_id, seq_no, event_type, data, create_time)
        values
        (#{eventId}, #{taskId}, #{seqNo}, #{eventType}, #{data}, #{createTime})
    </insert>

    <insert id="insertIfAbsent" parameterType="com.sky.entity.AgentEvent"
            useGeneratedKeys="true" keyProperty="id">
        insert ignore into agent_event
        (event_id, task_id, seq_no, event_type, data, create_time)
        values
        (#{eventId}, #{taskId}, #{seqNo}, #{eventType}, #{data}, now())
    </insert>

    <insert id="batchInsert" parameterType="java.util.List">
        insert into agent_event
        (event_id, task_id, seq_no, event_type, data, create_time)
        values
        <foreach collection="list" item="item" separator=",">
            (#{item.eventId}, #{item.taskId}, #{item.seqNo},
             #{item.eventType}, #{item.data}, #{item.createTime})
        </foreach>
    </insert>

</mapper>
